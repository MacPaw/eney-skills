{
  "name": "security-utilities-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "A MCP server for the password and other utilities.",
  "main": "index.js",
  "scripts": {
    "start": "tsx index.ts",
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepack": "npm run clean && npm run build && npm i --omit=dev && cp manifest.json dist/manifest.json && cp -r node_modules dist/node_modules",
    "pack": "mcpb pack dist security-utilities-mcp.mcpb",
    "postpack": "npm i"
  },
  "dependencies": {
    "@macpaw/eney-api": "^2.4.0-beta.3",
    "@modelcontextprotocol/sdk": "^1.24.3",
    "react": "^18.3.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/react": "^18.3.27",
    "typescript": "^5.9.3"
  }
}
