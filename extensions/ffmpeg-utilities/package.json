{
  "name": "ffmpeg-utilities",
  "version": "1.0.0",
  "description": "FFmpeg utilities extensions",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bundle": "node ../../cli/main.ts bundle",
    "build": "npm run typecheck && npm run bundle",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run typecheck && npm run bundle"
  },
  "dependencies": {
    "zod": "^4.1.0",
    "@macpaw/eney-api": "^1.1.0-beta.1"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/react": "^18.3.26",
    "typescript": "^5.6.3"
  }
}
