{
  "name": "image-utilities",
  "description": "Image utilities extensions",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bundle": "node ../../cli/main.ts bundle",
    "build": "npm run typecheck && npm run bundle",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run typecheck && npm run bundle"
  },
  "dependencies": {
    "@macpaw/eney-api": "^2.0.0",
    "react": "^18.0.0",
    "sharp": "^0.34.4",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/react": "^18.3.26",
    "typescript": "^5.6.3"
  }
}
